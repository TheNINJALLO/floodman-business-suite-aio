# Blockers

| ID | Scope | State | Blocking condition | Work that can continue |
|---|---|---|---|---|
| BLK-001 | Original execution history | RESOLVED | The requested master prompt and five ledgers were absent from the byte-verified handoff, and no prior Git metadata existed. | Reconstructed controls now record the handoff baseline explicitly. |
| BLK-002 | Shell verification on Windows | RESOLVED | `subprocess.run(["bash", ...])` invoked the denied Windows WSL shim despite Git Bash being first on `PATH`. | `verify_repo.py` now resolves the executable path explicitly on Windows; full verification passes with 0 warnings. |
| BLK-003 | Pinned RoomFlow source | RESOLVED | Exact commit `1f97817a52b916875e50cc6380c0d284072b8ce8` was fetched into the ignored vendor checkout and verified clean with 1,032 tracked files. | The reproducible fetch and validation scripts retain the pin; no upstream source is committed. |
| BLK-004 | iOS compilation | RESOLVED | 2026-08-18: GitHub Actions run `32197870130` used macOS 26/Xcode 26.6 to compile the unsigned iPhone/iPad simulator app, install it, execute 6/6 RoomFlow Capture XCTest methods with 0 failures, package it, and upload artifact `9346645878` (`sha256:4e135c2929c1a08cebca809e913ece03863baf75ab1d53b9b8eb082caac5a2ef`). | Physical RoomPlan/LiDAR/ARKit behavior remains BLK-006; signing, archive, TestFlight, and production remain BLK-007. |
| BLK-005 | Staging acceptance | OPEN | No approved staging Pterodactyl/Tailscale node or sanitized backup is present. A 2026-08-18 Wings log proved the first v4.7.0 launcher stopped on a stale RoomFlow editor assertion; the corrected launcher is locally verified but has not yet completed startup on that node. | Replace only the launcher for this failure, then validate capture sync, clean install/upgrade/restart, existing estimate/PDF flows, and backup/restore on an approved isolated node. |
| BLK-006 | Native device acceptance | OPEN | Physical Android/iPhone/iPad devices and public staging HTTPS are external. | Run Android Depth and non-Depth guided capture plus Apple RoomPlan/LiDAR and ARKit fallback, permission, weak-tracking, relocalization, offline replay, and interruption matrices from `ROOMFLOW_CAPTURE.md`. |
| BLK-007 | Signing and production release | OPEN | Apple/Android signing, provider credentials, store uploads, and production deployment require explicit approval. | Do not access credentials; finish pre-signing gates. |
| BLK-008 | Container advisory scan | OPEN | Docker Scout 1.23.1 did not return a local CVE report within five minutes, so no vulnerability result exists to evaluate. | The immutable clean build and isolated contract audit are complete. Re-run a critical/high scan in CI or a dedicated scanner and review the generated SBOM before any release. |
| BLK-009 | Current derivative image build | RESOLVED | 2026-08-18: GitHub Actions run `32197176318` verified immutable inputs, completed the clean Buildx build, authenticated to GHCR, published the v4.7.0 image, and recorded digest `sha256:f1dc722dc9326b6642bc1d981186ec87dce34de1d73c02efcca267aa7c5f75e6`. | The unavailable critical/high advisory result remains BLK-008; live startup, restart, and backup/restore remain BLK-005. |
| BLK-010 | Native CI dispatch | RESOLVED | 2026-08-18: the intended `TheNINJALLO/floodman-business-suite-aio` remote was configured and published over authenticated HTTPS without exposing the stored credential. Android run `32197176293`, Apple simulator run `32197870130`, and source verification run `32197870225` completed successfully. | Physical-device and signing/store work remain BLK-006/BLK-007. The GitHub CLI is still unavailable, but it is not required for authenticated Git/REST operation. |
| BLK-011 | RoomFlow Capture release identity and package | RESOLVED | 2026-08-18: the user explicitly authorized a distinct Android/web test release and later authorized GitHub publication and remote builds. | v4.7.0 web/Pterodactyl, Android 0.4.0-alpha01/build 13, and unsigned iOS alpha03 simulator artifacts are built and checksummed. BLK-005 through BLK-008 and BLK-012 still govern staging, devices, scans, signing/store, production, and installed-image reconciliation. |
| BLK-012 | Installed Pterodactyl base image drift | OPEN | The supplied Wings log pulled `ghcr.io/theninjallo/floodman-business-suite-aio:3.2.0` at digest `895fcf…`, while the reviewed v4.7.0 egg is pinned to AIO `3.2.2` at digest `3c2d61…`. No image change was authorized or attempted during diagnosis. | Back up the server and reconcile the egg/image setting during an approved maintenance or staging window; do not delete persistent data directories. |
| BLK-013 | Direct supplied Xactimate PLX extraction | OPEN | The exact 2,109,334-byte PLX (`sha256:8da48331…70e2`) is a valid ZIP with one opaque/protected 2,109,208-byte `XACTDOC.ZIPXML` member (`sha256:50839537…a613`). Official Verisk help documents compatible-profile Data Transfer and Price List Editor use but no public PLX payload schema or supported decoder was found. Guessing at or circumventing the protected payload would not be a faithful/licensed import. | Use Xactimate's supported Data Transfer/Price List Editor path and supply an authorized populated Floodman pricing CSV, or provide a documented/licensed Verisk integration/converter. The safe preview/confirm CSV importer and all unrelated estimate/catalog work continue. |
| BLK-014 | Post-v4.7.0 source release identity/package | RESOLVED | 2026-08-21: explicit owner approval assigned distinct server/web/Pterodactyl v4.7.1. Two builds are byte-identical; 205 internal runtime files, 87 launcher preflights, 12 portable package smokes, deployment hashes, and repository verification pass. The frozen v4.7.0 runtime (`733514…967a`) and launcher (`38687b…c6b4`) remain byte-identical. | Publish only the authorized feature branch/draft PR. BLK-005 through BLK-008 and BLK-012 still govern staging, devices, advisory scanning, signing/store, production, backup/restore, and installed-image reconciliation. |

An open blocker applies only to its named scope and does not pause unrelated tasks.
