# Blockers

| ID | Scope | State | Blocking condition | Work that can continue |
|---|---|---|---|---|
| BLK-001 | Original execution history | RESOLVED | The requested master prompt and five ledgers were absent from the byte-verified handoff, and no prior Git metadata existed. | Reconstructed controls now record the handoff baseline explicitly. |
| BLK-002 | Shell verification on Windows | RESOLVED | `subprocess.run(["bash", ...])` invoked the denied Windows WSL shim despite Git Bash being first on `PATH`. | `verify_repo.py` now resolves the executable path explicitly on Windows; full verification passes with 0 warnings. |
| BLK-003 | Pinned RoomFlow source | RESOLVED | Exact commit `1f97817a52b916875e50cc6380c0d284072b8ce8` was fetched into the ignored vendor checkout and verified clean with 1,032 tracked files. | The reproducible fetch and validation scripts retain the pin; no upstream source is committed. |
| BLK-004 | iOS compilation | OPEN | Xcode and an iOS simulator require a macOS runner. | 2026-08-18: 11 Swift files, RoomPlan/ARKit capture, six XCTest methods, project/assets, and the checksum-producing build/test workflow pass Windows readiness; run that workflow on macOS 26/Xcode 26. |
| BLK-005 | Staging acceptance | OPEN | No approved staging Pterodactyl/Tailscale node or sanitized backup is present. | Validate capture sync, clean install/upgrade/restart, existing estimate/PDF flows, and backup/restore on an approved isolated node. |
| BLK-006 | Native device acceptance | OPEN | Physical Android/iPhone/iPad devices and public staging HTTPS are external. | Run Android Depth and non-Depth guided capture plus Apple RoomPlan/LiDAR and ARKit fallback, permission, weak-tracking, relocalization, offline replay, and interruption matrices from `ROOMFLOW_CAPTURE.md`. |
| BLK-007 | Signing and production release | OPEN | Apple/Android signing, provider credentials, store uploads, and production deployment require explicit approval. | Do not access credentials; finish pre-signing gates. |
| BLK-008 | Container advisory scan | OPEN | Docker Scout 1.23.1 did not return a local CVE report within five minutes, so no vulnerability result exists to evaluate. | The immutable clean build and isolated contract audit are complete. Re-run a critical/high scan in CI or a dedicated scanner and review the generated SBOM before any release. |
| BLK-009 | Current derivative image build | OPEN | Docker Desktop is installed but its service is stopped/manual and the daemon did not respond. Starting the engine may automatically restart unrelated existing containers, which is outside DEC-016 and the authorized isolated test scope. | Build and test the Pterodactyl runtime/launcher/egg independently. Build and audit the derivative image on a clean runner or an owner-approved isolated Docker engine. |
| BLK-010 | Native CI dispatch | OPEN | This checkout has no Git remote and the GitHub CLI is unavailable, so there is no authenticated repository/workflow target to which the Android or unsigned iOS simulator build can be dispatched. | The current RoomFlow Capture Android gate and all Windows-available Apple readiness checks pass. Configure the intended repository remote before requesting an external workflow run. |
| BLK-011 | RoomFlow Capture release identity and package | OPEN | The existing v4.6.10 runtime and alpha12/alpha03 identities predate this candidate. Reusing them would publish different contents under already distributed versions, while the required Xcode/device/staging/container/backup gates are not complete. | Preserve the frozen v4.6.10 installer. After BLK-004 through BLK-010 are cleared or an explicit release exception is approved, assign distinct identities and regenerate packages, manifests, checksums, and release notes. |

An open blocker applies only to its named scope and does not pause unrelated tasks.
