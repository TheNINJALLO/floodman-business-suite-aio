# Capability matrix

What this pack actually covers, layer by layer, against a target architecture spanning creative direction through evaluation. Every "present" row cites a real file or constraint ID that exists today; every "absent" row was confirmed absent by direct search, not assumed. Where a row says "prose only," that means the guidance exists in a skill or reference but nothing machine-checks it — a real gap, not a rounding error.

**How to read enforcement type:**

| Type | Means |
|---|---|
| `AST` | Checked by `scripts/parser_constraints.js` via the TypeScript compiler API |
| `regex` | Checked by `scripts/test_constraints.py`'s `CONSTRAINTS` list |
| `browser` | Checked by Playwright + axe-core in `tools/screenshots/`, currently **outside** the blocking gate chain |
| `prose` | Documented in a skill/core file, not machine-checked |
| `absent` | Confirmed absent by search — no rule, tool, doc, or demo covers it |

**Priority:** P0 = foundational gap · P1 = high-impact · P2 = useful enhancement · P3 = experimental / logged for later.

This file is regenerated by hand today (there is no figure-gate coverage for a capability matrix — see the Gate 11 note at the bottom). Treat the counts it cites (skills, constraints, gates) as a snapshot tied to whatever `metadata.json` said at audit time; re-derive them from a green `npm run gates` before trusting them in a future session, per this project's own standing rule.

---

## Layer A — Creative / Design Intelligence

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Typography systems | `skills/design-system`, `core/design-tokens.md`, `TYP-01/02/03` | regex | — | Font-pairing data (`font_pairings: 57`) plus the `TYP-*` rule family |
| Color systems | `skills/color-themes`, `core/design-tokens.md`, `COL-01/03/04`, `TOK-01/02` | regex | — | OKLCH tokens enforced; raw-hex-in-token-definition banned |
| Spacing systems | `core/design-tokens.md` (4pt grid), `tools/token-report/token-report.mjs` | prose + report script | — | Not a gate — `token-report` flags off-4pt-scale and redundant arbitrary bracket values (`p-[13px]`, `gap-[1.1rem]`) as an informational report, never blocks a build |
| Composition / layout systems | `skills/design-principles`, `skills/landing-pages` | prose | — | Reference-depth guidance, not machine-checked (layout is inherently hard to verify statically) |
| Motion | `skills/animations`, `MOTION-01/02`, `ANI-03/04`, `DELAY-01` | AST + regex | — | 12 GSAP-plugin references, ease-curve and stagger-delay rules |
| Icon strategy | `skills/iconography` | prose + AST (`A11Y-07` icon-button label) | — | |
| Data visualization | `skills/data-tables` (chart guidance), 25 `chart_types` in reference data | prose | P2 | No rendered-chart accessibility check (see Layer K) |
| Content / UI copy | `skills/design-principles`, `COPY-01/02`, `SLOP-02` (AI-slop phrasing ban) | AST + regex | — | |
| Anti-generic-pattern guidance | Anti-slop wall (`SKILL.md`), `SLOP-01..06` | regex | P1 | Ban-list only — see Layer L's Anti-slop 2.0 entry for the context-aware gap |

## Layer B — Product UX

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Onboarding, dashboards, settings | `skills/data-tables`, `demo/dashboard` (stub-typed) | prose + example | P2 | Dashboard demo not provably renderable — see Phase 1d scope note |
| Search / filtering / nav | `skills/component-patterns`, `skills/web-interface` | prose | — | |
| Command palettes, notifications, dialogs | `skills/component-patterns` references | prose | P2 | |
| Multi-step flows, checkout | `skills/forms`, eval case `checkout-payment` | prose + eval | — | |
| Auth / account | `skills/forms`, `demo/auth-form` (stub-typed), eval case `auth-flow` | prose + eval | — | Accessible-authentication SC (WCAG 3.3.8) named in `core/accessibility-baseline.md` but not machine-checked — see Layer E |
| Empty / loading / error / success states | `STA-01`, `STA-02`, `docs/STATE_MATRIX.md` | regex + reference | — | `STA-01`/`STA-02` remain keyword-presence only (loading/error); empty/success stay unchecked by any gate — `docs/STATE_MATRIX.md` documents that honestly rather than overclaiming, see Layer K |
| Optimistic UI, undo, autosave, recovery | — | absent | P2 | No skill, rule, or demo covers this explicitly |
| Destructive actions, permissions, collaboration/presence | — | absent | P3 | Not covered; niche relative to the pack's current scope |

## Layer C — Implementation Architecture

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Semantic HTML | `A11Y-*` family, `skills/web-interface` | AST + regex | — | |
| Component API design | `core/component-api.md` + deep companion | prose + `COMP-01` (AST) | — | |
| Forms | `skills/forms`, `FORM-01` | regex | — | React Hook Form + Zod patterns |
| Server/client boundaries, App Router, RSC, Suspense, streaming, server actions | `skills/react-performance`, `skills/react-components` | prose | P1 | High-value, framework-current, but zero machine enforcement of RSC-boundary correctness |
| CSS architecture, design tokens, theming | `core/design-tokens.md`, `TOK-01/02` | regex | — | |
| Container queries | `skills/web-interface` reference | prose | P2 | |

## Layer D — Design-to-Code

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Figma → code, brand/token extraction | `skills/design-system/references/{figma-to-code,brand-extraction,token-interop}.md` | prose workflow | P1 | Real reference depth exists; nothing verifies an extraction's output against a source |
| Screenshot → code / design-system inference | — | absent | P2 | Not covered by any skill today |
| Design → state matrix, → component API, → a11y requirements | Partially implied by `skills/design-system` + `core/component-api.md` | prose | P2 | No explicit design-to-code pipeline artifact |
| Design → visual regression baseline | **New this pass** — `tools/screenshots/visual-regression.mjs` | browser | P0 → shipping | See Phase 1d |

## Layer E — Accessibility

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Real `aria-*` (not comment-only) | `A11Y-01` | AST | — | |
| Focus-visible on interactive elements | `A11Y-02` | AST | — | |
| `outline-none` without replacement | `A11Y-06` | regex | — | |
| Skip link | `A11Y-04` | regex | — | |
| Image alt / dimensions | `A11Y-05` (regex), `A11Y-03`/`IMG-01` (AST/regex) | AST + regex | — | |
| Icon-button label | `A11Y-07` | regex | — | |
| Form label association | `A11Y-08` | regex | — | |
| WCAG 2.2 focus-not-obscured (2.4.11), target-size (2.5.8), dragging-alternative (2.5.7), redundant-entry (3.3.7) | Named in `core/accessibility-baseline.md` prose | **prose only** | **P1** | Attempted this pass, deliberately not shipped as a rule. Target-size (2.5.8) needs an effective box size — content box plus padding plus, in practice, sibling spacing — that a regex or a single-element AST check cannot compute without a real layout engine; the reliable-looking shortcut (flag bare `h-4`/`w-4`-class utilities directly on an interactive element) still needs per-file judgment about compensating padding, which is exactly the "agent judgment still required" caveat `SLOP-04`'s own comment already warns against re-creating. Dragging-alternative (2.5.7) can detect `draggable`/`onDragStart`/dnd-library usage cleanly, but "has an alternative" has no reliable positive signal to check for — a false pass is worse than no check. Focus-not-obscured (2.4.11) and redundant-entry (3.3.7) are cross-element/cross-page properties (a sticky header colliding with focus, a value asked for twice across two different forms) that a single-file check structurally cannot see. All four stay human-review items; forcing a fragile rule to claim the row would repeat the exact failure this project already found and fixed once (`SLOP-04`, `docs/CHANGELOG.md`) rather than avoid it. |
| Accessible authentication (3.3.8) | `core/accessibility-baseline.md` | prose | P1 | |
| Runtime axe-core (WCAG 2.1 A/AA, serious/critical) | `tools/screenshots/verify.mjs`, `verify-home.mjs` | browser | P1 | Real and working, but outside the blocking gate chain — not run on every PR |
| APCA contrast | Named as "the better perceptual check" in `core/accessibility-baseline.md` | prose, explicitly aspirational | P2 | Doc states "WCAG 2 remains the shipping gate" |
| Keyboard-trap / screen-reader automated testing | — | absent beyond axe | P2 | |

## Layer F — Visual QA

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Screenshot capture (viewport/theme) | `tools/screenshots/capture.mjs`, `verify.mjs` | browser | — | Capture + smoke-check only |
| Console errors, hydration mismatches, horizontal overflow | `tools/screenshots/verify.mjs` (390/768/1920, dev+prod, light+dark) | browser | — | |
| **Visual regression / screenshot diffing** | **Was absent — shipped this pass** | browser | **P0 → shipped** | `tools/screenshots/visual-regression.mjs`, scoped to `home/` + `demo/landing-page` (3 viewports, one color scheme per target). `demo/showcase` excluded: its hero is a WebGL particle field seeded at random, so every capture differs from the last — a pixel diff against it would fail on every run regardless of any real change. Ships informational-only in CI. Full 9-viewport matrix, both themes per target, and a frozen-seed path for `showcase` are explicitly deferred, not silently skipped. |
| Component/page state visual coverage (hover/focus/active/disabled) | — | absent | P1 | Depends on Layer K's state-matrix work landing first |

## Layer G — Performance

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| No `transition: all` | `PERF-04`, `PERF-04R` | AST + regex | — | |
| No barrel imports | `PERF-01` | AST | — | |
| No numeric `&&` in JSX (hydration-mismatch risk) | `PERF-02` | AST | — | |
| Image width/height/fill | `IMG-01`, `A11Y-03` | regex + AST | — | |
| Core Web Vitals (LCP/INP/CLS) measurement | — | absent | **P0** | No Lighthouse, no field/lab CWV data anywhere in CI |
| JS bundle-size budget | — | absent | P1 | No bundle analyzer wired to any gate |
| Hydration-cost / unnecessary-render heuristics beyond `PERF-02` | `skills/react-performance` (70 `react_perf_rules` in reference data) | prose | P1 | Substantial reference depth, zero machine enforcement beyond the two AST rules above |

## Layer H — Security

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| `dangerouslySetInnerHTML` misuse | — | absent | P1 — attempted, dropped | A real stored-XSS bug was found in shipped reference prose by a one-time manual LLM audit (`docs/AUDIT.md`) and fixed by hand — never turned into a regression rule. Attempted this pass as `SEC-01`: 6 findings against the real corpus, all false positives (`home/app/page.tsx` injects build-time-generated CSS into a `<style>` tag, safe with no sanitizer call needed; `skills/ai-ui-generation/examples/good-registry-renderer.tsx` only mentions the API name in a comment). Whether the value reaching it is trusted/static or user-controlled is exactly the judgment a static regex cannot make. Full reasoning in `scripts/check_security.py`'s docstring. |
| Unsafe URL/href handling (`javascript:` scheme) | `scripts/check_security.py` — `SEC-02` | **script, shipped** | — | Proven via `scripts/test_security_patterns.py`: fires on 2 positive cases, silent on real-world negatives. Runs as `npm run security`, CI job `security` in `ci.yml`. 0 findings across 370 real files. |
| Unsafe `postMessage` (wildcard target origin) | `scripts/check_security.py` — `SEC-03` | **script, shipped** | — | Same proof harness as above. |
| Raw `innerHTML` assignment (vanilla DOM) | `scripts/check_security.py` — `SEC-04` | **script, shipped** | — | Same proof harness as above. |
| Open redirect | — | absent | P2 | Not attempted — needs knowledge of what counts as an internal vs. external destination, which is app-specific and not inferable from a single file |
| Client-side secret leakage, token storage mistakes | — | absent | P2 | |
| CSP-sensitive patterns, third-party script risk | — | absent | P3 | |

Shipped outside `test_constraints.py`'s `CONSTRAINTS` list and the `ci_constraints: 61` figure on purpose: adding entries there means recomputing that count across the ~35 documents that cite it, disproportionate to what 3 narrow patterns are worth on their own. Same reasoning as `visual-regression.mjs` sitting outside the 11 numbered gates. (`SLOP-06` did go through that sweep later this pass — see Layer L's Anti-slop 2.0 entry — because it extends an already-numbered category rather than introducing a new one; the proportionality judgment is about what kind of addition it is, not a rule against ever touching the count.)

## Layer I — Internationalization

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| RTL, logical properties, `dir` attribute, Tailwind `rtl:` variants | `skills/platform/references/i18n.md` | **prose only** | **P1** | Detailed reference exists; zero constraint IDs cite it, zero browser check exercises `dir="rtl"` |
| Locale-sensitive numbers/dates/currency, pluralization | `skills/platform/references/i18n.md` | prose | P2 | |
| Text-expansion / long-string layout resilience | — | absent as a demo/fixture | P1 | Target for Phase 2: one real fixture, not four languages |

## Layer J — Advanced Interaction / Motion

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Micro-interactions, spring motion, GSAP recipes | `skills/animations` (17 `animation_recipes`, 12 GSAP plugin refs) | prose + `MOTION-01/02` | — | |
| Scroll-driven effects | `skills/animations`, live example in `home/components/SectionHow.tsx` (ScrollTrigger scrub) | prose + example | — | |
| View transitions | 9 `view_transition_css_recipes` in reference data | prose | P2 | |
| Reduced-motion alternatives | Checked in `tools/screenshots/verify.mjs` matrix intent, enforced in examples via `motion-reduce:` patterns | browser (partial) + prose | — | |

## Layer K — Data-Dense UI

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Tables — sorting, filtering, pagination, bulk ops | `skills/data-tables`, `demo/dashboard` (stub-typed) | prose + example | — | |
| Virtualization, inline editing | `skills/data-tables` reference | prose | P2 | |
| Charts, legends, tooltips, chart a11y | 25 `chart_types` in reference data | prose | P2 | No accessible-chart machine check |
| **Formal state-matrix system** (component/form/data-page states as a reusable reference) | `docs/STATE_MATRIX.md`, cited against `skills/data-tables/examples/good-data-table.tsx` and `skills/forms/examples/good-checkout.tsx` | reference, shipped | — | Placed in `docs/`, not `core/`, to avoid the `core_files` figure cascade — nothing here is a core-dep. Ships a copy-paste-able TS discriminated-union type rather than a separate JSON Schema file, since nothing in this repo would consume an inert schema — the plan's original "+ JSON schema" framing is deliberately not followed literally |

## Layer L — Evaluation

| Capability | Coverage | Enforcement | Priority | Notes |
|---|---|---|---|---|
| Prompt-based eval suite | `evals/evals.json` — 22 cases, 161 assertions (`eval_tests: 22`, `semantic_assertions: 20` in `metadata.json`) | regex + semantic (LLM-judged) | — | Runs self-test mode in Gate 7; full semantic mode is budget-limited, not run every PR |
| Routing-accuracy eval | `evals/trigger-eval.json` — 30 cases (15 trigger, 15 not); `evals/trigger-queries.md` | eval — not gated (upstream harness, needs model calls) | P1 | Phase 1e cases added 2026-08-27: `design-research` positives (it had none in either direction), the ambiguous audit/build/redesign/screenshot-recreation intents, and matched adjacent negatives. Harness not re-run — `trigger-queries.md`'s 20/20 covers the original 20 only. Which-skill routing (vs. plain engage/don't-engage) needs an `expected_skill` field the binary harness lacks — still open |
| Golden-project benchmark suite (10 realistic apps spanning marketing/SaaS/ecommerce/auth/checkout/admin/mobile/editorial/dev-tool/settings) | 4 demos exist (`landing-page`, `showcase`, `dashboard`, `auth-form`), 2 of which are real installed apps | example (partial) | **P1 — logged, not built this pass** | Building the remaining 6 to the same rigor as `showcase` is a multi-week effort on its own; not attempted this pass. See "Deferred, explicitly" below. |
| Objective scoring model (visual/a11y/perf/security/architecture/copy weighted rubric) | — | absent | P2 — logged, not built this pass | Concrete starting point: don't build six new scorers — this pack already emits structured findings for four of the six axes (`check_security.py --json` → security, `test_constraints.py`'s pass ratio → architecture, `tools/screenshots/verify*.mjs`'s axe output → a11y, `tools/screenshots/visual-regression-report.json` → visual); a rubric is an aggregator over those, not new instrumentation. Perf and copy have no structured output today — perf has no check at all (see Layer G), copy is `SLOP-02`/`QUA-03` pass/fail with no graded signal |
| **Anti-slop 2.0** (context-aware — distinctive-and-appropriate, not merely rare) | Current `SLOP-01..06` are pattern bans, not context models | regex (partial) | **P1 — genuinely context-aware scoring rejected, not built** | Attempted and rejected this pass, honestly: "distinctive-and-appropriate, not merely rare" is a holistic judgment about whether a choice fits the brief — the same class of problem that kept WCAG 2.2 target-size and `SEC-01` dangerouslySetInnerHTML out of this pack's rules (see rows above and `docs/RESEARCH.md`). Forcing it into a regex would repeat `SLOP-04`'s own history. What *was* built instead: `SLOP-06` (no custom mouse cursors — `cursor-none` / `cursor: url(...)`), a literal, low-ambiguity extension of the existing ban-list pattern, chosen specifically because the wall bans it outright with no "unless the brief asks" carve-out, unlike the three AI-design defaults on the same line. Real corpus grep (`skills/`, `demo/`, `home/`, `core/`) found zero occurrences either way; proven instead via a self-test fixture (`scripts/test_constraints.py`'s `FIXTURE_BAD`). This is a real 61st constraint, not a standalone tool — the `ci_constraints`/`regex_constraints` figures and ~40 citing documents were swept via `npm run figures` to stay green, the same way every prior constraint addition in this pack's history has. |
| Design-token intelligence report (repeated/near-duplicate/inconsistent value detection) | `tools/token-report/token-report.mjs` | script, shipped | — | Report-only, exits 0 always; checks near-duplicate OKLCH tokens and off-scale/redundant arbitrary spacing (`p-[13px]` etc.) against the 4pt grid; proof harness `tools/token-report/token-report.test.mjs`, 9/9 passing, tuned against this repo's own real token files to avoid flagging deliberate near-duplicate contrast pairs |
| Skill/rule generator tooling | `scripts/scaffold.py` generates component boilerplate only, not skill/rule wiring | partial tool | P2 — logged, not built this pass | Premature to build before more rule patterns exist to generalize from |
| Benchmark reporting dashboard | — | absent | P3 — logged, not built this pass | Concrete starting point: sequenced *after* the golden-project benchmark suite and objective scoring model above, not before — a dashboard with nothing to aggregate is premature abstraction (the same reasoning declining the skill-generator row makes explicit). Once those exist it becomes a static-site render over their JSON outputs; `home/` already has a working Next.js static-export pipeline this could reuse rather than a new app |

---

## Deferred, explicitly (not silently)

These are real gaps this matrix identifies but that this pass does not attempt to close at full production rigor, because each is independently a multi-week effort and a shallow version would produce exactly the "files compile but nothing's actually verified" outcome this project's own culture (`docs/MAINTENANCE.md`, `CLAUDE.md`) explicitly warns against:

- Remaining 6 golden-project benchmark apps (Layer L)
- Objective scoring model / weighted rubric (Layer L)
- Skill/rule generator tooling, benchmark dashboards (Layer L)
- Screenshot → code / design-system inference (Layer D)
- Full Core Web Vitals field measurement (Layer G) — static/heuristic budgets are tractable now; real CWV data requires infrastructure this repo doesn't have (no deployed staging environment with RUM)
- Product-specific creative-direction system across 14 industries (Layer A, touched only glancingly via `brand_archetypes: 9` / `tone_directions: 14` in existing reference data)

## Note on this file's own figure-gate coverage

Every other count this project publishes (skills, constraints, gates, token depth) is recomputed from the filesystem by `scripts/check_figures.py` (Gate 11) — hand-editing one is a defect by this project's own standard. This file does not yet have that coverage: its "present/absent" judgments were derived by direct audit (three parallel codebase searches) rather than by a script, and its priority labels are a judgment call, not a computed value. That is a known limitation of the matrix itself, not swept under anything — a future pass could add a lightweight checker that at least verifies every "present" row's cited file/ID still exists, without attempting to automate the priority judgment.
