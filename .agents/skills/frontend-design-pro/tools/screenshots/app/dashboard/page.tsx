// Re-export only. The demo is imported where it lives so a capture is always of
// the committed source, never of a copy that has drifted from it.
export { default } from "../../../../demo/dashboard/app/page";
